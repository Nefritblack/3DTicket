&НаКлиенте
Процедура ПолучитьНовыйПарольПользователя(Результат, НеПрекращатьРаботу) Экспорт
	
	Если Результат = Неопределено Тогда 
		Если НеПрекращатьРаботу <> истина Тогда ПрекратитьРаботуСистемы() КонецЕсли;
	Иначе
		ОбщиеФункции.УстановитьПароль(Результат) КонецЕсли;
	
КонецПроцедуры
